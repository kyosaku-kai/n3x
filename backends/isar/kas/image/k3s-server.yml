# K3s Server Image configuration
# Builds an image with K3s Kubernetes control plane
#
# Usage: kas-container --isar build kas/base.yml:kas/machine/<target>.yml:kas/image/k3s-server.yml

header:
  version: 14

target: isar-k3s-image-server
