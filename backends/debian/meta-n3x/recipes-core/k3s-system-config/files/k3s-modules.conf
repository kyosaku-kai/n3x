# Kernel modules required for k3s and container networking
# Loaded at boot by systemd-modules-load.service

# Container runtime support
overlay

# Connection tracking for iptables
nf_conntrack

# Bridge netfilter for pod networking
br_netfilter

# NAT tables for pod networking
iptable_nat
iptable_filter

# IPv6 NAT (optional, but recommended)
ip6table_nat
ip6table_filter
